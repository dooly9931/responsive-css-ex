import React from "react";
import "./Favorites.scss";

const Favorites = (props) => {
  return (
    <div>
      <div id="favorites-page">Favorites</div>
      <button className="button-enable">Enable</button>
    </div>
  );
};

export default Favorites;
